{
  "name": "medicine-tracker-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Regression tests for medicine tracking mini-program + H5",
  "scripts": {
    "test": "node --test tests/**/*.test.js"
  }
}
