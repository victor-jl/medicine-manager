{
  "name": "medicine-manager-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Regression tests for the medicine-manager mini program. Uses Node's built-in test runner (node:test) so no extra dependencies are required.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
