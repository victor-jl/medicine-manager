{
  "name": "medicine-manager-tests",
  "version": "1.0.0",
  "description": "Test configuration for medicine manager mini program",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
