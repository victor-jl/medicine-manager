{
  "name": "medicine-tracker-tests",
  "version": "1.0.0",
  "description": "Test suite for medicine tracker mini-program",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
