{
  "name": "medicine-records-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Regression tests for medicine-records mini-program and H5 modules.",
  "scripts": {
    "test": "node --test --test-reporter=spec 'tests/*.test.js'",
    "test:single": "node --test --test-reporter=spec"
  }
}
